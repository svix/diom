{
  "private": true,
  "scripts": {
    "bump-version": "node scripts/bump-version.js",
    "check-version-sync": "node scripts/check-version-sync.js"
  },
  "devDependencies": {
    "semver": "^7.5.2"
  }
}
